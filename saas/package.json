{
  "name": "it-inventory-saas",
  "version": "0.1.0",
  "description": "Multi-tenant SaaS MVP for IT Inventory (auth, orgs, people, devices).",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "pilot:validate": "node --test test/multitenant-import.test.js",
    "check:staging": "node scripts/check-staging.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=22"
  }
}
