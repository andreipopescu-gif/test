{
  "name": "it-inventory-saas",
  "version": "0.1.0",
  "description": "Multi-tenant SaaS MVP for IT Inventory (auth, orgs, people, devices).",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
